package org.utbot.engine

import soot.RefType
import soot.SootMethod
import soot.jimple.JimpleBody

class MethodTraverser {
    fun test() {
        val method = SootMethod("123", emptyList(), RefType.v("t"))

            //        createSootMethod("setup", )
    }
}