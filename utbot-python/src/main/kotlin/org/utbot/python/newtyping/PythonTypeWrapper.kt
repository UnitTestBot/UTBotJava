package org.utbot.python.newtyping

/*
class PythonTypeWrapper private constructor(val type: Type) {
    override fun equals(other: Any?): Boolean {
        if (other !is PythonTypeWrapper)
            return false
        return super.equals(other)
    }

    override fun hashCode(): Int {
        if (type is PythonTypeVar) {
            
        }
    }

    companion object {
        fun create(type: Type): PythonTypeWrapper {
            if (!type.isPythonType())
                error("Trying to create PythonWrapper not of Python type")
            return PythonTypeWrapper(type)
        }
    }
}
 */