package org.utbot.python.providers

import org.utbot.framework.plugin.api.ClassId
import org.utbot.fuzzer.FuzzedMethodDescription
import org.utbot.fuzzer.FuzzedParameter
import org.utbot.fuzzer.ModelProvider

/*
class AnyTypeModelProvider(val suggestedTypes: List<Set<ClassId>>): ModelProvider {
    override fun generate(description: FuzzedMethodDescription): Sequence<FuzzedParameter> {
        suggestedTypes.forEachIndexed { index, classIds ->

        }
    }

}
 */